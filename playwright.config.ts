import { defineConfig } from "@playwright/test";

// Keep this Playwright config standard/minimal.
// This project primarily uses Vitest, but Playwright can still be run if tests exist.
export default defineConfig({});
